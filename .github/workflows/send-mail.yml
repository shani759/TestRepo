name: Send Email Test

on:
  workflow_dispatch:
    inputs:
      primary_email:
        description: 'Primary email address'
        required: true
        default: 'mazdaq.shahzad@spglobal.com'
      secondary_email:
        description: 'Secondary email address'
        required: true
        default: 'Zeeshan.Ajmal@spglobal.com'

jobs:
  send-email:
    runs-on: ubuntu-latest
    steps:
      - name: Send Test Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: "mazdaq.shahzad6@gmail.com"
          password: "vdtp bbzy gtep pwzt"
          subject: 'Test Email from GitHub Actions'
          body: 'This is a test email generated from GitHub Actions.'
          to: ${{ github.event.inputs.primary_email }},${{ github.event.inputs.secondary_email }}
